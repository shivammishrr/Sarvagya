"""Tool clients for external services (simple HTTP wrappers).

- browser_client: calls the Browser Tool service
- python_repl_client: calls the Python REPL service
- tavily_search: calls Tavily search API
"""
